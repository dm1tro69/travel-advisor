import React from 'react';
import useStyle from './styles'

const Map = () => {
    const classes = useStyle()
    return (
        <div>
            Map
        </div>
    );
};

export default Map;
