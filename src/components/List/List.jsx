import React from 'react';
import useStyle from './styles'

const List = () => {
    const classes = useStyle()
    return (
        <div>
            List
        </div>
    );
};

export default List;
