import React from 'react';
import useStyle from './styles'

const Header = () => {
    const classes = useStyle()
    return (
        <div>
            Header
        </div>
    );
};

export default Header;
