import React from 'react';
import useStyle from './styles'

const PlaceDetails = () => {
    const classes = useStyle()
    return (
        <div>
            PlaceDetails
        </div>
    );
};

export default PlaceDetails;
